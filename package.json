{
  "name": "saphircaisse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,front -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix front\"",
    "install:all": "npm install --prefix server && npm install --prefix front",
    "build": "npm run build --prefix front",
    "start:prod": "npm run start:prod --prefix server",
    "prod": "npm run build && npm run start:prod"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
